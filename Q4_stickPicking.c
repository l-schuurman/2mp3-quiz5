#include <stdio.h>
#include <stdlib.h>

int main() {
    int M, N;
    int *backpack = NULL; // Dynamic array to store stick lengths
    int size = 0; // Maximum number of sticks in the backpack
    int count = 0; // Current number of sticks in the backpack
    int sum = 0; // Total length of sticks in the backpack
    int stick;

    // YOU'LL WRITE SOME CODE HERE

    while (scanf("%d", &stick) != EOF) {
        // WRITE YOUR CODE HERE AS WELL
    }
    
    printf("\n");

    return 0;
}
